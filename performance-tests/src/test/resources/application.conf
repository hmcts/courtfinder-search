#########################
# Gatling Configuration #
#########################

# The default url for every service.
# For example

baseUrl = ${?APP_URL}
userName = ${?APP_NAME}
userPassword = ${?APP_PASSWORD}
    
users = 10
duration = 300

