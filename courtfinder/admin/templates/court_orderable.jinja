{% extends "court.jinja" %}

{% block details %}

    <a href="{{reorder_url}}">Reorder</a>
    <form method="post" action="{{return_url}}">
    {{ formset.management_form }}
    {% for form in formset %}

      <fieldset class="form-group">
        {% csrf_token %}
        {{ form.as_p }}
        {% if not forloop.last %}
        <hr>
        {% endif %}
      </fieldset>

    {% endfor %}

    <input type="submit" class="button" name="Update" value="Update" />
    </form>

{% endblock %}
